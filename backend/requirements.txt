# newsprism backend — pinned for Python 3.13 on Windows, CPU only.
#
# torch is pulled from the PyTorch CPU index. On Windows the PyPI wheel is
# already CPU-only, but pinning the index makes the intent explicit and keeps
# the install identical on Linux/CI, where the PyPI default is a CUDA build.
--extra-index-url https://download.pytorch.org/whl/cpu

# --- web ---
fastapi==0.118.0
uvicorn[standard]==0.37.0
pydantic==2.13.5

# --- ingestion ---
feedparser==6.0.11
httpx==0.28.1
python-dateutil==2.9.0.post0

# --- clustering ---
numpy==2.3.3
scikit-learn==1.7.2
torch==2.8.0+cpu
sentence-transformers==5.1.1
